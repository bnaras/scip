# Set objective sense

Set objective sense

## Usage

``` r
scip_set_objective_sense(model, sense = "minimize")
```

## Arguments

- model:

  A SCIP model.

- sense:

  Character; `"minimize"` (default) or `"maximize"`.

## Value

Invisible `NULL`.
