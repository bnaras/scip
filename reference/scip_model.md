# Create a SCIP model

Creates a new SCIP optimization model for incremental problem
construction.

## Usage

``` r
scip_model(name = "scip_model")
```

## Arguments

- name:

  Character; problem name. Default `"scip_model"`.

## Value

An external pointer representing the SCIP model.
