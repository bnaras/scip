# NA

## Task

1.  To build a solver interface for R along the lines of the other
    solvers on CRAN
2.  Example solver interfaces are in `/Users/naras/GitHub` under
    `osqp-r`, `clarabel-r`, `piqp-r` and the older `ECOSolveR`.
3.  We need to ensure that the solver should be pluggable into `CVXR`
    (`/Users/naras/research/cvxr/new_design`).
4.  The source for the SCIP solver is in the `src` directory.
