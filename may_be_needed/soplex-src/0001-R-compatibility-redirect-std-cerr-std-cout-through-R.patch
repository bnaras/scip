From faaf4db694808d9f6d5a51804b69d815106c43d5 Mon Sep 17 00:00:00 2001
From: Balasubramanian Narasimhan <b.naras@gmail.com>
Date: Sat, 7 Mar 2026 16:11:14 -0800
Subject: [PATCH 1/5] R compatibility: redirect std::cerr/std::cout through R
 I/O

- Add RStreambuf in spxout.cpp to redirect stream output through Rprintf/REprintf
- Replace std::cerr usage with REprintf in spxdefines.cpp
- Add extern declarations in other files
---
 .gitmodules               |  3 ++
 src/papilo                |  1 +
 src/soplex/didxset.cpp    |  5 +++
 src/soplex/idxset.cpp     |  5 +++
 src/soplex/nameset.cpp    |  5 +++
 src/soplex/spxdefines.cpp |  8 +++--
 src/soplex/spxout.cpp     | 67 +++++++++++++++++++++++++++++++++++++--
 src/soplex_interface.cpp  |  5 +++
 8 files changed, 95 insertions(+), 4 deletions(-)
 create mode 100644 .gitmodules
 create mode 160000 src/papilo

diff --git a/.gitmodules b/.gitmodules
new file mode 100644
index 00000000..0da71d54
--- /dev/null
+++ b/.gitmodules
@@ -0,0 +1,3 @@
+[submodule "src/papilo"]
+	path = src/papilo
+	url = https://github.com/scipopt/papilo.git
diff --git a/src/papilo b/src/papilo
new file mode 160000
index 00000000..83660e41
--- /dev/null
+++ b/src/papilo
@@ -0,0 +1 @@
+Subproject commit 83660e41be8d5befb0713afb5b5739ab5dcb8221
diff --git a/src/soplex/didxset.cpp b/src/soplex/didxset.cpp
index 706a242b..0205e99b 100644
--- a/src/soplex/didxset.cpp
+++ b/src/soplex/didxset.cpp
@@ -25,6 +25,11 @@
 #include "soplex/didxset.h"
 #include "soplex/spxalloc.h"
 
+extern "C" {
+extern void Rprintf(const char *, ...);
+extern void REprintf(const char *, ...);
+}
+
 namespace soplex
 {
 
diff --git a/src/soplex/idxset.cpp b/src/soplex/idxset.cpp
index b435bcf7..475a21a1 100644
--- a/src/soplex/idxset.cpp
+++ b/src/soplex/idxset.cpp
@@ -24,6 +24,11 @@
 
 #include "soplex/idxset.h"
 
+extern "C" {
+extern void Rprintf(const char *, ...);
+extern void REprintf(const char *, ...);
+}
+
 namespace soplex
 {
 
diff --git a/src/soplex/nameset.cpp b/src/soplex/nameset.cpp
index 17b1f74c..9e9ca76a 100644
--- a/src/soplex/nameset.cpp
+++ b/src/soplex/nameset.cpp
@@ -27,6 +27,11 @@
 #include "soplex/nameset.h"
 #include "soplex/spxalloc.h"
 
+extern "C" {
+extern void Rprintf(const char *, ...);
+extern void REprintf(const char *, ...);
+}
+
 namespace soplex
 {
 const char NameSet::Name::deflt = '\0';
diff --git a/src/soplex/spxdefines.cpp b/src/soplex/spxdefines.cpp
index abadb749..5e330e61 100644
--- a/src/soplex/spxdefines.cpp
+++ b/src/soplex/spxdefines.cpp
@@ -30,6 +30,11 @@
 #include "soplex/spxout.h"
 #include "soplex/rational.h"
 
+extern "C" {
+extern void Rprintf(const char *, ...);
+extern void REprintf(const char *, ...);
+}
+
 namespace soplex
 {
 // Overloaded EQ function
@@ -46,8 +51,7 @@ bool msginconsistent(const char* name, const char* file, int line)
    assert(file != nullptr);
    assert(line >= 0);
 
-   SPX_MSG_ERROR(std::cerr << file << "(" << line << ") "
-                 << "Inconsistency detected in " << name << std::endl;)
+   SPX_MSG_ERROR(REprintf("%s(%d) Inconsistency detected in %s\n", file, line, name);)
 
    return 0;
 }
diff --git a/src/soplex/spxout.cpp b/src/soplex/spxout.cpp
index 3971e7d0..66de6497 100644
--- a/src/soplex/spxout.cpp
+++ b/src/soplex/spxout.cpp
@@ -22,10 +22,73 @@
 /*                                                                           */
 /* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * */
 
+#include <streambuf>
+#include <ostream>
+#include <cstring>
 #include "soplex/spxout.h"
 #include "soplex/exceptions.h"
 #include "soplex/spxalloc.h"
 
+extern "C" {
+extern void Rprintf(const char *, ...);
+extern void REprintf(const char *, ...);
+}
+
+namespace
+{
+
+/// A streambuf that forwards output to Rprintf (stdout) or REprintf (stderr).
+class RStreambuf : public std::streambuf
+{
+public:
+   /// If \p useStderr is true, output goes to REprintf; otherwise Rprintf.
+   explicit RStreambuf(bool useStderr) : m_useStderr(useStderr) {}
+
+protected:
+   int overflow(int c) override
+   {
+      if(c != EOF)
+      {
+         char ch = static_cast<char>(c);
+         if(m_useStderr)
+            REprintf("%c", ch);
+         else
+            Rprintf("%c", ch);
+      }
+      return c;
+   }
+
+   std::streamsize xsputn(const char* s, std::streamsize n) override
+   {
+      // Write in chunks to avoid issues with non-null-terminated data
+      char buf[1024];
+      std::streamsize remaining = n;
+      while(remaining > 0)
+      {
+         std::streamsize chunk = (remaining < 1023) ? remaining : 1023;
+         std::memcpy(buf, s, static_cast<size_t>(chunk));
+         buf[chunk] = '\0';
+         if(m_useStderr)
+            REprintf("%s", buf);
+         else
+            Rprintf("%s", buf);
+         s += chunk;
+         remaining -= chunk;
+      }
+      return n;
+   }
+
+private:
+   bool m_useStderr;
+};
+
+static RStreambuf s_errBuf(true);
+static RStreambuf s_outBuf(false);
+static std::ostream s_errStream(&s_errBuf);
+static std::ostream s_outStream(&s_outBuf);
+
+} // anonymous namespace
+
 namespace soplex
 {
 /// constructor
@@ -35,10 +98,10 @@ SPxOut::SPxOut()
 {
    spx_alloc(m_streams, VERB_INFO3 + 1);
    m_streams = new(m_streams) std::ostream*[VERB_INFO3 + 1];
-   m_streams[ VERB_ERROR ] = m_streams[ VERB_WARNING ] = &std::cerr;
+   m_streams[ VERB_ERROR ] = m_streams[ VERB_WARNING ] = &s_errStream;
 
    for(int i = VERB_DEBUG; i <= VERB_INFO3; ++i)
-      m_streams[ i ] = &std::cout;
+      m_streams[ i ] = &s_outStream;
 }
 
 //---------------------------------------------------
diff --git a/src/soplex_interface.cpp b/src/soplex_interface.cpp
index 606d60e8..011f8113 100644
--- a/src/soplex_interface.cpp
+++ b/src/soplex_interface.cpp
@@ -2,6 +2,11 @@
 #include "soplex_interface.h"
 #include <iostream>
 
+extern "C" {
+extern void Rprintf(const char *, ...);
+extern void REprintf(const char *, ...);
+}
+
 using namespace soplex;
 
 /** creates new SoPlex struct **/
-- 
2.53.0

